package com.mycompany.mavenproject17;
public interface MyFirstInterface 
{
    
    public static final int x = 5;
    
    void display();
    //1)Declaring variable with or withiout public static final keywords doesn't have any different because by default interface variables are public statis final.
    //2)Declaring abstract with or withiout abstract keyword doesn't have any different because by default interface methods are abstract. 
   
}
